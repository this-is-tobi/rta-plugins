# s3

S3-compatible object storage: buckets, objects, presigned URLs and policy

## Capabilities

| Capability         | Safety      | Summary                                                              |
|--------------------|-------------|----------------------------------------------------------------------|
| s3.bucket.download | write       | Copy a bucket to a local directory, for a person at a terminal       |
| s3.bucket.list     | read        | List every bucket the configured credentials can see                 |
| s3.bucket.upload   | destructive | Copy a local directory into a bucket, for a person at a terminal     |
| s3.object.copy     | write       | Copy an object to a new bucket/key                                   |
| s3.object.get      | write       | Download an object's content                                         |
| s3.object.list     | read        | List objects in a bucket                                             |
| s3.object.presign  | write       | Generate a time-limited URL for an object                            |
| s3.object.rename   | write       | Move an object to a new bucket/key                                   |
| s3.object.rm       | destructive | Delete an object                                                     |
| s3.object.set      | write       | Upload (or overwrite) an object                                      |
| s3.object.show     | read        | Show one object's metadata — size, type, etag — never its content    |
| s3.object.tree     | read        | The shape of a bucket in one call, with objects and bytes per prefix |
| s3.overview        | read        | Endpoint and bucket count at a glance                                |
| s3.policy.get      | read        | Show a bucket's policy document                                      |

## Configuration

Under `plugins: s3:` in rta's configuration, or in a profile's `set:`. An installed plugin's section is pinned to the artifact — `plugins: s3@<digest>:` — and `rta doctor` prints the exact line. The caller always wins, so a configured value is a default, never a lock.

| Key           | Read by                                                                                                                                                                                                                           | Help                                                                          |
|---------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------|
| access-key    | s3.bucket.download, s3.bucket.list, s3.bucket.upload, s3.object.copy, s3.object.get, s3.object.list, s3.object.presign, s3.object.rename, s3.object.rm, s3.object.set, s3.object.show, s3.object.tree, s3.overview, s3.policy.get | access key ID                                                                 |
| bucket        | s3.bucket.download, s3.bucket.upload, s3.object.copy, s3.object.get, s3.object.list, s3.object.presign, s3.object.rename, s3.object.rm, s3.object.set, s3.object.show, s3.object.tree, s3.policy.get                              | bucket to copy                                                                |
| ca-file       | s3.bucket.download, s3.bucket.list, s3.bucket.upload, s3.object.copy, s3.object.get, s3.object.list, s3.object.presign, s3.object.rename, s3.object.rm, s3.object.set, s3.object.show, s3.object.tree, s3.overview, s3.policy.get | PEM bundle to verify the server against, beyond the host's own trust store    |
| depth         | s3.object.tree                                                                                                                                                                                                                    | how many levels to expand                                                     |
| endpoint      | s3.bucket.download, s3.bucket.list, s3.bucket.upload, s3.object.copy, s3.object.get, s3.object.list, s3.object.presign, s3.object.rename, s3.object.rm, s3.object.set, s3.object.show, s3.object.tree, s3.overview, s3.policy.get | S3-compatible endpoint, host\[:port\]                                         |
| limit         | s3.bucket.download, s3.bucket.upload, s3.object.list, s3.object.tree                                                                                                                                                              | maximum objects to copy; a larger bucket is refused, not truncated            |
| parallel      | s3.bucket.download, s3.bucket.upload                                                                                                                                                                                              | how many objects to download at once                                          |
| prefix        | s3.bucket.download, s3.bucket.upload, s3.object.list, s3.object.tree                                                                                                                                                              | only objects under this prefix; empty means the whole bucket                  |
| presign.ttl   | s3.object.presign                                                                                                                                                                                                                 | seconds the URL stays valid — S3's own cap is 7 days (604800)                 |
| recursive     | s3.object.list                                                                                                                                                                                                                    | ignore the "/" delimiter and list everything under prefix                     |
| region        | s3.bucket.download, s3.bucket.list, s3.bucket.upload, s3.object.copy, s3.object.get, s3.object.list, s3.object.presign, s3.object.rename, s3.object.rm, s3.object.set, s3.object.show, s3.object.tree, s3.overview, s3.policy.get | bucket region                                                                 |
| storage-class | s3.object.set                                                                                                                                                                                                                     | e.g. STANDARD, STANDARD_IA, GLACIER — left to the server's default if omitted |
| tls           | s3.bucket.download, s3.bucket.list, s3.bucket.upload, s3.object.copy, s3.object.get, s3.object.list, s3.object.presign, s3.object.rename, s3.object.rm, s3.object.set, s3.object.show, s3.object.tree, s3.overview, s3.policy.get | use HTTPS (a local MinIO ordinarily does not)                                 |

## s3.bucket.download

Every object under a prefix, onto local disk, in parallel. **Refuses MCP outright** rather than asking for a grant — the line keys.backup and kv.copy draw, and the one pg.dump and vault.snapshot follow: a whole bucket has no blast radius a grant could name. An agent that needs an object asks for s3.object.get with a grant naming that key.

**An object key becomes a filename, and the key comes from the server.** `../../../../etc/cron.d/root` is a legal S3 key, so every destination is resolved and checked to be inside the directory you named; a key that escapes refuses the whole download and names the keys rather than skipping them quietly, because a backup missing the interesting objects is worse than one that did not run. The listing is checked in full before anything is written, so a refusal costs no partial directory.

Written into a directory this creates — never one that already exists, so a backup is never half of one run and half of another — at mode 0700 with each object at 0600. A run that fails takes the whole directory with it. --parallel is the flag that changes the transfer rate: object storage is latency-bound per object, so a bucket of many small files goes as fast as you are willing to ask for at once.

| Field                | Value                                                                                                                                                                                                                                                                           |
|----------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| id                   | s3.bucket.download                                                                                                                                                                                                                                                              |
| summary              | Copy a bucket to a local directory, for a person at a terminal                                                                                                                                                                                                                  |
| safety               | write                                                                                                                                                                                                                                                                           |
| idempotent           | false                                                                                                                                                                                                                                                                           |
| cli                  | rta s3 bucket download \[--bucket \<string>\] \[--prefix \<string>\] \[--out \<path>\] \[--parallel \<int>\] \[--limit \<int>\] \[--endpoint \<string>\] \[--region \<string>\] \[--tls \<bool>\] \[--access-key \<string>\] \[--secret-key \<secret>\] \[--ca-file \<string>\] |
| mcp-tool             | none — for the person at the terminal, never an agent                                                                                                                                                                                                                           |
| grant required (mcp) | yes — a person must run \`rta grant allow s3.bucket.download\`                                                                                                                                                                                                                  |
| profiles             | --profile \<name> runs this against a configured connection; over MCP that always needs \`rta grant allow s3 --profile \<name>\`                                                                                                                                                |
| input:bucket         | string, required, completes, from config plugins.s3.bucket — bucket to copy                                                                                                                                                                                                     |
| input:prefix         | string, completes, from config plugins.s3.prefix — only objects under this prefix; empty means the whole bucket                                                                                                                                                                 |
| input:out            | path, local (never offered to MCP callers) — directory to create and write into; refused if it already exists                                                                                                                                                                   |
| input:parallel       | int, default 8, from config plugins.s3.parallel — how many objects to download at once                                                                                                                                                                                          |
| input:limit          | int, default 10000, from config plugins.s3.limit — maximum objects to copy; a larger bucket is refused, not truncated                                                                                                                                                           |
| input:endpoint       | string, default 127.0.0.1:9000, local (never offered to MCP callers), from config plugins.s3.endpoint, filled by a profile's tunnel (the forward's address) — S3-compatible endpoint, host\[:port\]                                                                             |
| input:region         | string, default us-east-1, local (never offered to MCP callers), from config plugins.s3.region — bucket region                                                                                                                                                                  |
| input:tls            | bool, default false, local (never offered to MCP callers), from config plugins.s3.tls, filled by a profile's tunnel (the forward's tls) — use HTTPS (a local MinIO ordinarily does not)                                                                                         |
| input:access-key     | string, local (never offered to MCP callers), from config plugins.s3.access-key — access key ID                                                                                                                                                                                 |
| input:secret-key     | secret, local (never offered to MCP callers), from $RTA_S3_SECRET_KEY — secret access key                                                                                                                                                                                       |
| input:ca-file        | string, default , local (never offered to MCP callers), from config plugins.s3.ca-file — PEM bundle to verify the server against, beyond the host's own trust store                                                                                                             |

## s3.bucket.list

| Field            | Value                                                                                                                                                                                               |
|------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| id               | s3.bucket.list                                                                                                                                                                                      |
| summary          | List every bucket the configured credentials can see                                                                                                                                                |
| safety           | read                                                                                                                                                                                                |
| idempotent       | true                                                                                                                                                                                                |
| cli              | rta s3 bucket list \[--endpoint \<string>\] \[--region \<string>\] \[--tls \<bool>\] \[--access-key \<string>\] \[--secret-key \<secret>\] \[--ca-file \<string>\]                                  |
| mcp-tool         | s3_bucket_list                                                                                                                                                                                      |
| profiles         | --profile \<name> runs this against a configured connection; over MCP that always needs \`rta grant allow s3 --profile \<name>\`                                                                    |
| input:endpoint   | string, default 127.0.0.1:9000, local (never offered to MCP callers), from config plugins.s3.endpoint, filled by a profile's tunnel (the forward's address) — S3-compatible endpoint, host\[:port\] |
| input:region     | string, default us-east-1, local (never offered to MCP callers), from config plugins.s3.region — bucket region                                                                                      |
| input:tls        | bool, default false, local (never offered to MCP callers), from config plugins.s3.tls, filled by a profile's tunnel (the forward's tls) — use HTTPS (a local MinIO ordinarily does not)             |
| input:access-key | string, local (never offered to MCP callers), from config plugins.s3.access-key — access key ID                                                                                                     |
| input:secret-key | secret, local (never offered to MCP callers), from $RTA_S3_SECRET_KEY — secret access key                                                                                                           |
| input:ca-file    | string, default , local (never offered to MCP callers), from config plugins.s3.ca-file — PEM bundle to verify the server against, beyond the host's own trust store                                 |

## s3.bucket.upload

Every regular file under a directory, into a bucket, in parallel — s3.bucket.download's other half, and what restores one of its backups. **Refuses MCP outright** for the download's reason run in reverse: everything arriving instead of everything leaving, and no blast radius a grant could name. An agent that needs to write an object asks for s3.object.set with a grant naming that key.

**A destination already holding objects under the prefix is refused unless --overwrite says that is the point** — the download's fresh-directory rule pointing the other way. --overwrite replaces objects whose keys collide and leaves the rest, and the receipt says so.

Regular files only: a symlink refuses the whole upload by name — a link pointing at a credential would ship it as faithfully as any file, and a backup directory this plugin wrote contains no links, so one appearing deserves a person looking. Refused past --limit rather than truncated. A failed upload deletes nothing remote: a delete could also destroy what --overwrite already replaced, so the error names the possibly-partial prefix and the operator decides.

| Field                | Value                                                                                                                                                                                                                                                                                      |
|----------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| id                   | s3.bucket.upload                                                                                                                                                                                                                                                                           |
| summary              | Copy a local directory into a bucket, for a person at a terminal                                                                                                                                                                                                                           |
| safety               | destructive                                                                                                                                                                                                                                                                                |
| idempotent           | false                                                                                                                                                                                                                                                                                      |
| cli                  | rta s3 bucket upload \[--bucket \<string>\] \<dir> \[--prefix \<string>\] \[--overwrite \<bool>\] \[--parallel \<int>\] \[--limit \<int>\] \[--endpoint \<string>\] \[--region \<string>\] \[--tls \<bool>\] \[--access-key \<string>\] \[--secret-key \<secret>\] \[--ca-file \<string>\] |
| mcp-tool             | none — for the person at the terminal, never an agent                                                                                                                                                                                                                                      |
| grant required (mcp) | yes — a person must run \`rta grant allow s3.bucket.upload\`, optionally naming one bucket                                                                                                                                                                                                 |
| profiles             | --profile \<name> runs this against a configured connection; over MCP that always needs \`rta grant allow s3 --profile \<name>\`                                                                                                                                                           |
| input:bucket         | string, required, completes, from config plugins.s3.bucket — bucket to upload into                                                                                                                                                                                                         |
| input:dir            | path, required, local (never offered to MCP callers) — directory whose files become objects — what s3.bucket.download wrote                                                                                                                                                                |
| input:prefix         | string, completes, from config plugins.s3.prefix — key prefix to upload under; empty means the bucket root                                                                                                                                                                                 |
| input:overwrite      | bool — allow replacing remote objects whose keys collide; others are left as they are                                                                                                                                                                                                      |
| input:parallel       | int, default 8, from config plugins.s3.parallel — how many objects to upload at once                                                                                                                                                                                                       |
| input:limit          | int, default 10000, from config plugins.s3.limit — maximum files to upload; a larger directory is refused, not truncated                                                                                                                                                                   |
| input:endpoint       | string, default 127.0.0.1:9000, local (never offered to MCP callers), from config plugins.s3.endpoint, filled by a profile's tunnel (the forward's address) — S3-compatible endpoint, host\[:port\]                                                                                        |
| input:region         | string, default us-east-1, local (never offered to MCP callers), from config plugins.s3.region — bucket region                                                                                                                                                                             |
| input:tls            | bool, default false, local (never offered to MCP callers), from config plugins.s3.tls, filled by a profile's tunnel (the forward's tls) — use HTTPS (a local MinIO ordinarily does not)                                                                                                    |
| input:access-key     | string, local (never offered to MCP callers), from config plugins.s3.access-key — access key ID                                                                                                                                                                                            |
| input:secret-key     | secret, local (never offered to MCP callers), from $RTA_S3_SECRET_KEY — secret access key                                                                                                                                                                                                  |
| input:ca-file        | string, default , local (never offered to MCP callers), from config plugins.s3.ca-file — PEM bundle to verify the server against, beyond the host's own trust store                                                                                                                        |

## s3.object.copy

Copies server-side; the content never passes through this process. Refuses if --dest-key already exists rather than writing over it: a grant names the source key and says nothing about the object it would replace.

| Field                | Value                                                                                                                                                                                                                                                 |
|----------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| id                   | s3.object.copy                                                                                                                                                                                                                                        |
| summary              | Copy an object to a new bucket/key                                                                                                                                                                                                                    |
| safety               | write                                                                                                                                                                                                                                                 |
| idempotent           | false                                                                                                                                                                                                                                                 |
| cli                  | rta s3 object copy \[--bucket \<string>\] \<key> \[--dest-bucket \<string>\] \[--dest-key \<string>\] \[--endpoint \<string>\] \[--region \<string>\] \[--tls \<bool>\] \[--access-key \<string>\] \[--secret-key \<secret>\] \[--ca-file \<string>\] |
| mcp-tool             | s3_object_copy                                                                                                                                                                                                                                        |
| grant required (mcp) | yes — a person must run \`rta grant allow s3.object.copy\`, optionally naming one key                                                                                                                                                                 |
| profiles             | --profile \<name> runs this against a configured connection; over MCP that always needs \`rta grant allow s3 --profile \<name>\`                                                                                                                      |
| input:bucket         | string, required, completes, from config plugins.s3.bucket — source bucket                                                                                                                                                                            |
| input:key            | string, required, completes — object to copy                                                                                                                                                                                                          |
| input:dest-bucket    | string, completes, local (never offered to MCP callers) — destination bucket; same as --bucket if omitted                                                                                                                                             |
| input:dest-key       | string, required — destination object name                                                                                                                                                                                                            |
| input:endpoint       | string, default 127.0.0.1:9000, local (never offered to MCP callers), from config plugins.s3.endpoint, filled by a profile's tunnel (the forward's address) — S3-compatible endpoint, host\[:port\]                                                   |
| input:region         | string, default us-east-1, local (never offered to MCP callers), from config plugins.s3.region — bucket region                                                                                                                                        |
| input:tls            | bool, default false, local (never offered to MCP callers), from config plugins.s3.tls, filled by a profile's tunnel (the forward's tls) — use HTTPS (a local MinIO ordinarily does not)                                                               |
| input:access-key     | string, local (never offered to MCP callers), from config plugins.s3.access-key — access key ID                                                                                                                                                       |
| input:secret-key     | secret, local (never offered to MCP callers), from $RTA_S3_SECRET_KEY — secret access key                                                                                                                                                             |
| input:ca-file        | string, default , local (never offered to MCP callers), from config plugins.s3.ca-file — PEM bundle to verify the server against, beyond the host's own trust store                                                                                   |

## s3.object.get

Writes the content to stdout with no framing; for the byte-exact copy, or anything binary, --out writes it to a file (0600) instead — a person's flag only, since a grant authorizes revealing the content, not choosing where on this machine it lands. An MCP caller always gets the content back in the response, bounded the same way http.get bounds a response body. --out never overwrites: a destination that already exists is refused, and a download that fails partway removes what it wrote.

| Field                | Value                                                                                                                                                                                                             |
|----------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| id                   | s3.object.get                                                                                                                                                                                                     |
| summary              | Download an object's content                                                                                                                                                                                      |
| safety               | write                                                                                                                                                                                                             |
| idempotent           | true                                                                                                                                                                                                              |
| cli                  | rta s3 object get \[--bucket \<string>\] \<key> \[--out \<path>\] \[--endpoint \<string>\] \[--region \<string>\] \[--tls \<bool>\] \[--access-key \<string>\] \[--secret-key \<secret>\] \[--ca-file \<string>\] |
| mcp-tool             | s3_object_get                                                                                                                                                                                                     |
| grant required (mcp) | yes — a person must run \`rta grant allow s3.object.get\`, optionally naming one key                                                                                                                              |
| profiles             | --profile \<name> runs this against a configured connection; over MCP that always needs \`rta grant allow s3 --profile \<name>\`                                                                                  |
| input:bucket         | string, required, completes, from config plugins.s3.bucket — bucket the object is in                                                                                                                              |
| input:key            | string, required, completes — object to reveal                                                                                                                                                                    |
| input:out            | path, local (never offered to MCP callers) — write the content to this file instead of printing it (refused if it exists)                                                                                         |
| input:endpoint       | string, default 127.0.0.1:9000, local (never offered to MCP callers), from config plugins.s3.endpoint, filled by a profile's tunnel (the forward's address) — S3-compatible endpoint, host\[:port\]               |
| input:region         | string, default us-east-1, local (never offered to MCP callers), from config plugins.s3.region — bucket region                                                                                                    |
| input:tls            | bool, default false, local (never offered to MCP callers), from config plugins.s3.tls, filled by a profile's tunnel (the forward's tls) — use HTTPS (a local MinIO ordinarily does not)                           |
| input:access-key     | string, local (never offered to MCP callers), from config plugins.s3.access-key — access key ID                                                                                                                   |
| input:secret-key     | secret, local (never offered to MCP callers), from $RTA_S3_SECRET_KEY — secret access key                                                                                                                         |
| input:ca-file        | string, default , local (never offered to MCP callers), from config plugins.s3.ca-file — PEM bundle to verify the server against, beyond the host's own trust store                                               |

## s3.object.list

Grouped by "/" like a directory listing unless --recursive flattens the whole prefix.

Bounded: a bucket can hold millions of keys, so this returns --limit of them and says what to pass to --after for the next page. A listing that stopped and did not say so reads exactly like a bucket with that little in it.

| Field            | Value                                                                                                                                                                                                                                                                             |
|------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| id               | s3.object.list                                                                                                                                                                                                                                                                    |
| summary          | List objects in a bucket                                                                                                                                                                                                                                                          |
| safety           | read                                                                                                                                                                                                                                                                              |
| idempotent       | true                                                                                                                                                                                                                                                                              |
| cli              | rta s3 object list \[--bucket \<string>\] \[--prefix \<string>\] \[--recursive \<bool>\] \[--limit \<int>\] \[--after \<string>\] \[--endpoint \<string>\] \[--region \<string>\] \[--tls \<bool>\] \[--access-key \<string>\] \[--secret-key \<secret>\] \[--ca-file \<string>\] |
| mcp-tool         | s3_object_list                                                                                                                                                                                                                                                                    |
| profiles         | --profile \<name> runs this against a configured connection; over MCP that always needs \`rta grant allow s3 --profile \<name>\`                                                                                                                                                  |
| input:bucket     | string, required, completes, from config plugins.s3.bucket — bucket to list                                                                                                                                                                                                       |
| input:prefix     | string, completes, from config plugins.s3.prefix — only keys starting with this                                                                                                                                                                                                   |
| input:recursive  | bool, from config plugins.s3.recursive — ignore the "/" delimiter and list everything under prefix                                                                                                                                                                                |
| input:limit      | int, default 200, from config plugins.s3.limit — how many objects to return                                                                                                                                                                                                       |
| input:after      | string — continue from the key the last page ended at                                                                                                                                                                                                                             |
| input:endpoint   | string, default 127.0.0.1:9000, local (never offered to MCP callers), from config plugins.s3.endpoint, filled by a profile's tunnel (the forward's address) — S3-compatible endpoint, host\[:port\]                                                                               |
| input:region     | string, default us-east-1, local (never offered to MCP callers), from config plugins.s3.region — bucket region                                                                                                                                                                    |
| input:tls        | bool, default false, local (never offered to MCP callers), from config plugins.s3.tls, filled by a profile's tunnel (the forward's tls) — use HTTPS (a local MinIO ordinarily does not)                                                                                           |
| input:access-key | string, local (never offered to MCP callers), from config plugins.s3.access-key — access key ID                                                                                                                                                                                   |
| input:secret-key | secret, local (never offered to MCP callers), from $RTA_S3_SECRET_KEY — secret access key                                                                                                                                                                                         |
| input:ca-file    | string, default , local (never offered to MCP callers), from config plugins.s3.ca-file — PEM bundle to verify the server against, beyond the host's own trust store                                                                                                               |

## s3.object.presign

The URL itself is a credential: anyone who has it can act on the object until --ttl expires, with no further authentication and no further grant check — this call is the one gated moment, not each use of the link. --method put grants write access to a caller-chosen key instead of read access to an existing one.

| Field                | Value                                                                                                                                                                                                                                       |
|----------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| id                   | s3.object.presign                                                                                                                                                                                                                           |
| summary              | Generate a time-limited URL for an object                                                                                                                                                                                                   |
| safety               | write                                                                                                                                                                                                                                       |
| idempotent           | false                                                                                                                                                                                                                                       |
| cli                  | rta s3 object presign \[--bucket \<string>\] \<key> \[--method \<string>\] \[--ttl \<int>\] \[--endpoint \<string>\] \[--region \<string>\] \[--tls \<bool>\] \[--access-key \<string>\] \[--secret-key \<secret>\] \[--ca-file \<string>\] |
| mcp-tool             | s3_object_presign                                                                                                                                                                                                                           |
| grant required (mcp) | yes — a person must run \`rta grant allow s3.object.presign\`, optionally naming one key                                                                                                                                                    |
| profiles             | --profile \<name> runs this against a configured connection; over MCP that always needs \`rta grant allow s3 --profile \<name>\`                                                                                                            |
| input:bucket         | string, required, completes, from config plugins.s3.bucket — bucket the object is in                                                                                                                                                        |
| input:key            | string, required, completes — object to presign                                                                                                                                                                                             |
| input:method         | string, default get, one of: get\|put — get for a download link, put for an upload link                                                                                                                                                     |
| input:ttl            | int, default 900, from config plugins.s3.presign.ttl — seconds the URL stays valid — S3's own cap is 7 days (604800)                                                                                                                        |
| input:endpoint       | string, default 127.0.0.1:9000, local (never offered to MCP callers), from config plugins.s3.endpoint, filled by a profile's tunnel (the forward's address) — S3-compatible endpoint, host\[:port\]                                         |
| input:region         | string, default us-east-1, local (never offered to MCP callers), from config plugins.s3.region — bucket region                                                                                                                              |
| input:tls            | bool, default false, local (never offered to MCP callers), from config plugins.s3.tls, filled by a profile's tunnel (the forward's tls) — use HTTPS (a local MinIO ordinarily does not)                                                     |
| input:access-key     | string, local (never offered to MCP callers), from config plugins.s3.access-key — access key ID                                                                                                                                             |
| input:secret-key     | secret, local (never offered to MCP callers), from $RTA_S3_SECRET_KEY — secret access key                                                                                                                                                   |
| input:ca-file        | string, default , local (never offered to MCP callers), from config plugins.s3.ca-file — PEM bundle to verify the server against, beyond the host's own trust store                                                                         |

## s3.object.rename

S3 has no native rename — this copies server-side, then removes the source. If the copy succeeds and the remove fails, the object exists in both places and the failure says so rather than reporting success.

| Field                | Value                                                                                                                                                                                                                                                   |
|----------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| id                   | s3.object.rename                                                                                                                                                                                                                                        |
| summary              | Move an object to a new bucket/key                                                                                                                                                                                                                      |
| safety               | write                                                                                                                                                                                                                                                   |
| idempotent           | false                                                                                                                                                                                                                                                   |
| cli                  | rta s3 object rename \[--bucket \<string>\] \<key> \[--dest-bucket \<string>\] \[--dest-key \<string>\] \[--endpoint \<string>\] \[--region \<string>\] \[--tls \<bool>\] \[--access-key \<string>\] \[--secret-key \<secret>\] \[--ca-file \<string>\] |
| mcp-tool             | s3_object_rename                                                                                                                                                                                                                                        |
| grant required (mcp) | yes — a person must run \`rta grant allow s3.object.rename\`, optionally naming one key                                                                                                                                                                 |
| profiles             | --profile \<name> runs this against a configured connection; over MCP that always needs \`rta grant allow s3 --profile \<name>\`                                                                                                                        |
| input:bucket         | string, required, completes, from config plugins.s3.bucket — source bucket                                                                                                                                                                              |
| input:key            | string, required, completes — object to move                                                                                                                                                                                                            |
| input:dest-bucket    | string, completes, local (never offered to MCP callers) — destination bucket; same as --bucket if omitted                                                                                                                                               |
| input:dest-key       | string, required — destination object name                                                                                                                                                                                                              |
| input:endpoint       | string, default 127.0.0.1:9000, local (never offered to MCP callers), from config plugins.s3.endpoint, filled by a profile's tunnel (the forward's address) — S3-compatible endpoint, host\[:port\]                                                     |
| input:region         | string, default us-east-1, local (never offered to MCP callers), from config plugins.s3.region — bucket region                                                                                                                                          |
| input:tls            | bool, default false, local (never offered to MCP callers), from config plugins.s3.tls, filled by a profile's tunnel (the forward's tls) — use HTTPS (a local MinIO ordinarily does not)                                                                 |
| input:access-key     | string, local (never offered to MCP callers), from config plugins.s3.access-key — access key ID                                                                                                                                                         |
| input:secret-key     | secret, local (never offered to MCP callers), from $RTA_S3_SECRET_KEY — secret access key                                                                                                                                                               |
| input:ca-file        | string, default , local (never offered to MCP callers), from config plugins.s3.ca-file — PEM bundle to verify the server against, beyond the host's own trust store                                                                                     |

## s3.object.rm

No history, no backup, no undo — the same loss `kv rm` is. S3's DELETE is idempotent: removing a key that is already gone is not an error, on this or the real call — --dry-run does not probe for existence first, since that would report a failure the real call would not.

| Field                | Value                                                                                                                                                                                               |
|----------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| id                   | s3.object.rm                                                                                                                                                                                        |
| summary              | Delete an object                                                                                                                                                                                    |
| safety               | destructive                                                                                                                                                                                         |
| idempotent           | false                                                                                                                                                                                               |
| cli                  | rta s3 object rm \[--bucket \<string>\] \<key> \[--endpoint \<string>\] \[--region \<string>\] \[--tls \<bool>\] \[--access-key \<string>\] \[--secret-key \<secret>\] \[--ca-file \<string>\]      |
| mcp-tool             | s3_object_rm                                                                                                                                                                                        |
| grant required (mcp) | yes — a person must run \`rta grant allow s3.object.rm\`, optionally naming one key                                                                                                                 |
| profiles             | --profile \<name> runs this against a configured connection; over MCP that always needs \`rta grant allow s3 --profile \<name>\`                                                                    |
| input:bucket         | string, required, completes, from config plugins.s3.bucket — bucket the object is in                                                                                                                |
| input:key            | string, required, completes — object to delete                                                                                                                                                      |
| input:endpoint       | string, default 127.0.0.1:9000, local (never offered to MCP callers), from config plugins.s3.endpoint, filled by a profile's tunnel (the forward's address) — S3-compatible endpoint, host\[:port\] |
| input:region         | string, default us-east-1, local (never offered to MCP callers), from config plugins.s3.region — bucket region                                                                                      |
| input:tls            | bool, default false, local (never offered to MCP callers), from config plugins.s3.tls, filled by a profile's tunnel (the forward's tls) — use HTTPS (a local MinIO ordinarily does not)             |
| input:access-key     | string, local (never offered to MCP callers), from config plugins.s3.access-key — access key ID                                                                                                     |
| input:secret-key     | secret, local (never offered to MCP callers), from $RTA_S3_SECRET_KEY — secret access key                                                                                                           |
| input:ca-file        | string, default , local (never offered to MCP callers), from config plugins.s3.ca-file — PEM bundle to verify the server against, beyond the host's own trust store                                 |

## s3.object.set

The content comes from the argument or from --file; PutObject handles large files with multipart upload internally, so there is no separate multipart capability to reach for.

| Field                | Value                                                                                                                                                                                                                                                                                   |
|----------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| id                   | s3.object.set                                                                                                                                                                                                                                                                           |
| summary              | Upload (or overwrite) an object                                                                                                                                                                                                                                                         |
| safety               | write                                                                                                                                                                                                                                                                                   |
| idempotent           | true                                                                                                                                                                                                                                                                                    |
| cli                  | rta s3 object set \[--bucket \<string>\] \<key> \[value\] \[--file \<path>\] \[--content-type \<string>\] \[--storage-class \<string>\] \[--endpoint \<string>\] \[--region \<string>\] \[--tls \<bool>\] \[--access-key \<string>\] \[--secret-key \<secret>\] \[--ca-file \<string>\] |
| mcp-tool             | s3_object_set                                                                                                                                                                                                                                                                           |
| grant required (mcp) | yes — a person must run \`rta grant allow s3.object.set\`, optionally naming one key                                                                                                                                                                                                    |
| profiles             | --profile \<name> runs this against a configured connection; over MCP that always needs \`rta grant allow s3 --profile \<name>\`                                                                                                                                                        |
| input:bucket         | string, required, completes, from config plugins.s3.bucket — bucket to write to                                                                                                                                                                                                         |
| input:key            | string, required, completes — object to set                                                                                                                                                                                                                                             |
| input:value          | text — content to upload                                                                                                                                                                                                                                                                |
| input:file           | path, local (never offered to MCP callers) — upload this file's content instead                                                                                                                                                                                                         |
| input:content-type   | string, completes — MIME type; guessed from --file's extension if omitted                                                                                                                                                                                                               |
| input:storage-class  | string, completes, from config plugins.s3.storage-class — e.g. STANDARD, STANDARD_IA, GLACIER — left to the server's default if omitted                                                                                                                                                 |
| input:endpoint       | string, default 127.0.0.1:9000, local (never offered to MCP callers), from config plugins.s3.endpoint, filled by a profile's tunnel (the forward's address) — S3-compatible endpoint, host\[:port\]                                                                                     |
| input:region         | string, default us-east-1, local (never offered to MCP callers), from config plugins.s3.region — bucket region                                                                                                                                                                          |
| input:tls            | bool, default false, local (never offered to MCP callers), from config plugins.s3.tls, filled by a profile's tunnel (the forward's tls) — use HTTPS (a local MinIO ordinarily does not)                                                                                                 |
| input:access-key     | string, local (never offered to MCP callers), from config plugins.s3.access-key — access key ID                                                                                                                                                                                         |
| input:secret-key     | secret, local (never offered to MCP callers), from $RTA_S3_SECRET_KEY — secret access key                                                                                                                                                                                               |
| input:ca-file        | string, default , local (never offered to MCP callers), from config plugins.s3.ca-file — PEM bundle to verify the server against, beyond the host's own trust store                                                                                                                     |

## s3.object.show

| Field            | Value                                                                                                                                                                                               |
|------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| id               | s3.object.show                                                                                                                                                                                      |
| summary          | Show one object's metadata — size, type, etag — never its content                                                                                                                                   |
| safety           | read                                                                                                                                                                                                |
| idempotent       | true                                                                                                                                                                                                |
| cli              | rta s3 object show \[--bucket \<string>\] \<key> \[--endpoint \<string>\] \[--region \<string>\] \[--tls \<bool>\] \[--access-key \<string>\] \[--secret-key \<secret>\] \[--ca-file \<string>\]    |
| mcp-tool         | s3_object_show                                                                                                                                                                                      |
| profiles         | --profile \<name> runs this against a configured connection; over MCP that always needs \`rta grant allow s3 --profile \<name>\`                                                                    |
| input:bucket     | string, required, completes, from config plugins.s3.bucket — bucket the object is in                                                                                                                |
| input:key        | string, required, completes — object to inspect                                                                                                                                                     |
| input:endpoint   | string, default 127.0.0.1:9000, local (never offered to MCP callers), from config plugins.s3.endpoint, filled by a profile's tunnel (the forward's address) — S3-compatible endpoint, host\[:port\] |
| input:region     | string, default us-east-1, local (never offered to MCP callers), from config plugins.s3.region — bucket region                                                                                      |
| input:tls        | bool, default false, local (never offered to MCP callers), from config plugins.s3.tls, filled by a profile's tunnel (the forward's tls) — use HTTPS (a local MinIO ordinarily does not)             |
| input:access-key | string, local (never offered to MCP callers), from config plugins.s3.access-key — access key ID                                                                                                     |
| input:secret-key | secret, local (never offered to MCP callers), from $RTA_S3_SECRET_KEY — secret access key                                                                                                           |
| input:ca-file    | string, default , local (never offered to MCP callers), from config plugins.s3.ca-file — PEM bundle to verify the server against, beyond the host's own trust store                                 |

## s3.object.tree

`s3 object list` groups on "/" and answers one level at a time, so learning what is in somebody else's bucket means retyping --prefix a level deeper over and over. This reads the prefix once and draws the whole shape.

Every prefix carries its recursive object count and total size, which is the question a flat paginated listing cannot answer: where the space went.

Names and sizes only, never content — the same Read/Write split s3.object.list and s3.object.get already draw.

One request, however deep the result: the folders are built here from the keys, not fetched a level at a time. Bounded on how much of the stream is read, and it says when it stopped rather than looking like a smaller bucket.

| Field            | Value                                                                                                                                                                                                                                                  |
|------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| id               | s3.object.tree                                                                                                                                                                                                                                         |
| summary          | The shape of a bucket in one call, with objects and bytes per prefix                                                                                                                                                                                   |
| safety           | read                                                                                                                                                                                                                                                   |
| idempotent       | true                                                                                                                                                                                                                                                   |
| cli              | rta s3 object tree \[--bucket \<string>\] \[--prefix \<string>\] \[--depth \<int>\] \[--limit \<int>\] \[--endpoint \<string>\] \[--region \<string>\] \[--tls \<bool>\] \[--access-key \<string>\] \[--secret-key \<secret>\] \[--ca-file \<string>\] |
| mcp-tool         | s3_object_tree                                                                                                                                                                                                                                         |
| profiles         | --profile \<name> runs this against a configured connection; over MCP that always needs \`rta grant allow s3 --profile \<name>\`                                                                                                                       |
| input:bucket     | string, required, completes, from config plugins.s3.bucket — bucket to walk                                                                                                                                                                            |
| input:prefix     | string, completes, from config plugins.s3.prefix — start here rather than at the root                                                                                                                                                                  |
| input:depth      | int, default 4, from config plugins.s3.depth — how many levels to expand                                                                                                                                                                               |
| input:limit      | int, default 2000, from config plugins.s3.limit — how many keys to read before stopping                                                                                                                                                                |
| input:endpoint   | string, default 127.0.0.1:9000, local (never offered to MCP callers), from config plugins.s3.endpoint, filled by a profile's tunnel (the forward's address) — S3-compatible endpoint, host\[:port\]                                                    |
| input:region     | string, default us-east-1, local (never offered to MCP callers), from config plugins.s3.region — bucket region                                                                                                                                         |
| input:tls        | bool, default false, local (never offered to MCP callers), from config plugins.s3.tls, filled by a profile's tunnel (the forward's tls) — use HTTPS (a local MinIO ordinarily does not)                                                                |
| input:access-key | string, local (never offered to MCP callers), from config plugins.s3.access-key — access key ID                                                                                                                                                        |
| input:secret-key | secret, local (never offered to MCP callers), from $RTA_S3_SECRET_KEY — secret access key                                                                                                                                                              |
| input:ca-file    | string, default , local (never offered to MCP callers), from config plugins.s3.ca-file — PEM bundle to verify the server against, beyond the host's own trust store                                                                                    |

## s3.overview

Whether this endpoint is reachable at all, and how many buckets the configured credentials can see. --detail adds the bucket list, with region and age, to the same page.

| Field            | Value                                                                                                                                                                                               |
|------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| id               | s3.overview                                                                                                                                                                                         |
| summary          | Endpoint and bucket count at a glance                                                                                                                                                               |
| safety           | read                                                                                                                                                                                                |
| idempotent       | true                                                                                                                                                                                                |
| cli              | rta s3 overview \[--endpoint \<string>\] \[--region \<string>\] \[--tls \<bool>\] \[--access-key \<string>\] \[--secret-key \<secret>\] \[--ca-file \<string>\] \[--detail\]                        |
| mcp-tool         | s3_overview                                                                                                                                                                                         |
| profiles         | --profile \<name> runs this against a configured connection; over MCP that always needs \`rta grant allow s3 --profile \<name>\`                                                                    |
| input:endpoint   | string, default 127.0.0.1:9000, local (never offered to MCP callers), from config plugins.s3.endpoint, filled by a profile's tunnel (the forward's address) — S3-compatible endpoint, host\[:port\] |
| input:region     | string, default us-east-1, local (never offered to MCP callers), from config plugins.s3.region — bucket region                                                                                      |
| input:tls        | bool, default false, local (never offered to MCP callers), from config plugins.s3.tls, filled by a profile's tunnel (the forward's tls) — use HTTPS (a local MinIO ordinarily does not)             |
| input:access-key | string, local (never offered to MCP callers), from config plugins.s3.access-key — access key ID                                                                                                     |
| input:secret-key | secret, local (never offered to MCP callers), from $RTA_S3_SECRET_KEY — secret access key                                                                                                           |
| input:ca-file    | string, default , local (never offered to MCP callers), from config plugins.s3.ca-file — PEM bundle to verify the server against, beyond the host's own trust store                                 |
| input:detail     | bool, default false — return the full detailed view instead of the compact summary                                                                                                                  |

## s3.policy.get

| Field            | Value                                                                                                                                                                                               |
|------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| id               | s3.policy.get                                                                                                                                                                                       |
| summary          | Show a bucket's policy document                                                                                                                                                                     |
| safety           | read                                                                                                                                                                                                |
| idempotent       | true                                                                                                                                                                                                |
| cli              | rta s3 policy get \[--bucket \<string>\] \[--endpoint \<string>\] \[--region \<string>\] \[--tls \<bool>\] \[--access-key \<string>\] \[--secret-key \<secret>\] \[--ca-file \<string>\]            |
| mcp-tool         | s3_policy_get                                                                                                                                                                                       |
| profiles         | --profile \<name> runs this against a configured connection; over MCP that always needs \`rta grant allow s3 --profile \<name>\`                                                                    |
| input:bucket     | string, required, completes, from config plugins.s3.bucket — bucket to inspect                                                                                                                      |
| input:endpoint   | string, default 127.0.0.1:9000, local (never offered to MCP callers), from config plugins.s3.endpoint, filled by a profile's tunnel (the forward's address) — S3-compatible endpoint, host\[:port\] |
| input:region     | string, default us-east-1, local (never offered to MCP callers), from config plugins.s3.region — bucket region                                                                                      |
| input:tls        | bool, default false, local (never offered to MCP callers), from config plugins.s3.tls, filled by a profile's tunnel (the forward's tls) — use HTTPS (a local MinIO ordinarily does not)             |
| input:access-key | string, local (never offered to MCP callers), from config plugins.s3.access-key — access key ID                                                                                                     |
| input:secret-key | secret, local (never offered to MCP callers), from $RTA_S3_SECRET_KEY — secret access key                                                                                                           |
| input:ca-file    | string, default , local (never offered to MCP callers), from config plugins.s3.ca-file — PEM bundle to verify the server against, beyond the host's own trust store                                 |
